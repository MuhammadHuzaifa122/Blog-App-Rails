%h1 Blog Posts

= link_to "New Post", new_post_path, class: "btn btn-primary mb-3"

%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Content
      %th Actions
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= truncate(post.content, length: 100)
        %td
          = link_to 'Show', post, class: 'btn btn-sm btn-info me-2'
          = link_to 'Edit', edit_post_path(post), class: 'btn btn-sm btn-warning me-2'
          = link_to 'Delete', post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger'
